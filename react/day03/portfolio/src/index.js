import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// 루트 엘리먼트를 가져와 ReactDOM으로 렌더링합니다.
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);